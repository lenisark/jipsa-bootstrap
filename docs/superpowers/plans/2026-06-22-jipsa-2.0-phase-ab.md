# jipsa 2.0 — Phase A(작업 객체) + B(승인 게이트) 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 휘발성 슬랙 대화를 SQLite 작업 객체(A)로 승격하고, 그 위에 개인 채널용 슬랙 버튼 승인 게이트(B)를 얹어 — 민감 도구 호출 직전 폰으로 `[승인][거부]` 후 진행되게 한다. 전부 로컬·슬랙 네이티브, `gate.enabled` 플래그로 즉시 롤백.

**Architecture:** `daemon.py`의 형제 파일로 `tasks.py`(SQLite task store)·`approval.py`(토큰·Block Kit·verdict)를 추가한다(기존 `reminders.py`와 동일 패턴). 승인 게이트의 랑데부 지점은 단일 SQLite 파일 `jipsa.db` — PreToolUse 훅(`pretooluse_gate.py`, Claude가 띄우는 별도 프로세스)이 `approvals` 행을 INSERT 후 status가 바뀔 때까지 DB를 폴링하고, 데몬은 슬랙 버튼 클릭(block_actions)을 받아 행을 UPDATE한다. 메모리 결합이 없어 데몬·세션이 죽었다 살아나도 DB에서 복구된다.

**Tech Stack:** Python 3 (stdlib `sqlite3`, `unittest`) · `slack_sdk` (Socket Mode, Block Kit) · Claude Code PreToolUse hook · 외부 의존성 0 추가.

---

## 핵심 설계 결정 (구현 전 반드시 읽을 것)

1. **파일 위치 — 설계 트리 대신 기존 관례 따름.** 설계 C-2는 `lib/tasks.py`를 그리지만, 실제 레포는 `reminders.py`가 `daemon.py`의 형제이고 `import reminders`로 로드된다. 따라서 `tasks.py`·`approval.py`·`pretooluse_gate.py`도 모두 `templates/scripts/slack-jipsa/`에 형제 파일로 둔다. 공유 `lib/`(notion·slack_mrkdwn)는 건드리지 않는다.

2. **DB 경로 — 기존 상태 파일과 co-locate.** 설계는 `~/.claude/jipsa/jipsa.db`라 하지만 `reminders.json`·`polls.json`·`wiki_saved.json`이 모두 `~/.claude/scripts/slack-jipsa/`에 있다. 일관성을 위해 `BASE / 'jipsa.db'` (BASE = `~/.claude/scripts/slack-jipsa`) 사용. 훅(별도 프로세스)도 같은 절대경로를 계산해 접근.

3. **PreToolUse 훅 계약 (공식 문서 확인 완료, https://code.claude.com/docs/en/hooks):**
   - `claude --print` 헤드리스 + `--dangerously-skip-permissions`/`bypassPermissions`에서도 PreToolUse **발동함**.
   - 차단: `{"hookSpecificOutput":{"hookEventName":"PreToolUse","permissionDecision":"deny","permissionDecisionReason":"..."}}` 를 stdout, exit 0. (또는 exit 2 + stderr.)
   - 허용: 같은 형식에 `"permissionDecision":"allow"`, exit 0.
   - stdin payload: `{session_id, transcript_path, cwd, permission_mode, hook_event_name, tool_name, tool_input}`. `tool_input`은 도구별로 다름(Bash→`command`, Write→`file_path`/`content`, Edit→파일정보).
   - 훅 기본 타임아웃 600초 → settings의 `timeout`으로 연장. 게이트 타임아웃(분) × 60 + 여유(60s)보다 크게 설정.

4. **CLAUDE_SKIP_HOOKS 충돌 해소.** 현재 `_run_claude`는 `env['CLAUDE_SKIP_HOOKS']='1'`로 **모든 훅을 끈다**(daemon.py:257). 게이트가 발동하려면 이 채널 호출에서만 이 줄을 빼야 한다. → `_run_claude(..., gate=False)` 인자 추가: 게이트 켜진 채널의 메인 채팅 호출만 `gate=True`로 불러 `CLAUDE_SKIP_HOOKS`를 설정하지 않고 게이트 env/settings를 주입한다. summarize/pin 등 다른 `_run_claude` 호출은 `gate=False`(기본) → 기존 동작 100% 보존.
   - **부작용 경고:** `CLAUDE_SKIP_HOOKS`를 빼면 사용자가 `~/.claude/settings.json`에 등록해 둔 Stop 훅도 발동할 수 있다. 데몬의 노션 적재는 `upsert_by_external_id`(ext_id=`jipsa:{channel}:{ts}`)로 멱등이라 파괴적 충돌은 없으나, 중복 행 가능. Task B7 스모크 단계에서 확인하고, 문제 시 게이트 settings에 빈 `Stop` 훅을 추가해 무력화한다.

5. **게이트 발동 범위(Phase B):** 개인 채널만(`access:owner`, `cwd:slack-jipsa`). 부서 escalate는 Phase C(범위 외).

6. **하위호환 = 최우선 불변식.** `channels.json`에 신규 필드가 없거나 `gate.enabled:false`/`tasks_enabled:false`면 1.x 동작과 바이트 동일해야 한다. 모든 신규 분기는 플래그 뒤에 둔다.

7. **테스트 전략.** `tasks.py`·`approval.py`는 순수 SQLite/토큰 로직 → stdlib `unittest`로 TDD(레포 루트 `tests/`, 외부 의존 0). 슬랙/클로드가 얽힌 daemon·hook·settings·docs는 단위테스트 불가 → 각 Task의 명시적 스모크 검증 단계로 대신한다.

---

## File Structure

| 파일 | 책임 | 신규/변경 |
|---|---|---|
| `templates/scripts/slack-jipsa/tasks.py` | SQLite task store: 스키마 init, create/get/list/set_state, 상태기계 검증 | 신규 |
| `templates/scripts/slack-jipsa/approval.py` | 토큰 발급·검증(단일사용·승인자 바인딩), Block Kit 카드, verdict CAS 적용, 만료 sweep | 신규 |
| `templates/scripts/slack-jipsa/pretooluse_gate.py` | PreToolUse 훅(별도 프로세스): stdin 파싱 → approvals INSERT + 슬랙 카드 게시 → DB 폴링 → allow/deny JSON 출력 | 신규 |
| `templates/scripts/slack-jipsa/.claude/settings.json.tmpl` | 게이트 채널용 PreToolUse 훅 등록(matcher=Bash\|Write\|Edit, timeout 연장) | 신규 |
| `templates/scripts/slack-jipsa/daemon.py` | `load_channels` 필드 확장 / `_run_claude` gate 인자 / block_actions 핸들러 / 작업 명령 / 만료 sweeper 스레드 | 변경 |
| `templates/scripts/slack-jipsa/channels.json.example` | `mission`·`tasks_enabled`·`gate` 필드 예시 추가 | 변경 |
| `tests/test_tasks.py` | tasks.py 단위테스트 | 신규 |
| `tests/test_approval.py` | approval.py 단위테스트 | 신규 |
| `modules/08-task-layer.md` | 작업 객체 셋업 AI 가이드 | 신규 |
| `modules/09-approval-gate.md` | 승인 게이트 셋업 AI 가이드 | 신규 |
| `SKILL.md` / `README.md` | 신규 모듈/기능 반영 | 변경 |

---

# PHASE A — 작업 객체 (순수 추가, 1.x 동작 무변)

## Task A1: `tasks.py` — SQLite task store

**Files:**
- Create: `templates/scripts/slack-jipsa/tasks.py`
- Test: `tests/test_tasks.py`

- [ ] **Step 1: 실패하는 테스트 작성** — `tests/test_tasks.py`

```python
import os, tempfile, unittest, importlib.util
from pathlib import Path

# 템플릿 디렉토리에서 tasks.py 를 직접 로드 (설치 없이 테스트)
SRC = Path(__file__).resolve().parents[1] / 'templates/scripts/slack-jipsa/tasks.py'

def load_tasks(db_path):
    spec = importlib.util.spec_from_file_location('tasks_under_test', SRC)
    mod = importlib.util.module_from_spec(spec)
    spec.loader.exec_module(mod)
    mod.DB_PATH = Path(db_path)          # 테스트 격리: 임시 DB로 교체
    mod.init_db()
    return mod

class TasksTest(unittest.TestCase):
    def setUp(self):
        self.tmp = tempfile.mkdtemp()
        self.t = load_tasks(os.path.join(self.tmp, 'jipsa.db'))

    def test_create_and_get(self):
        tid = self.t.create_task('C1', '정산 정리', body='6월분', direction='h2a')
        row = self.t.get_task(tid)
        self.assertEqual(row['title'], '정산 정리')
        self.assertEqual(row['state'], '대기')
        self.assertEqual(row['channel_id'], 'C1')
        self.assertEqual(row['direction'], 'h2a')
        self.assertEqual(row['assignee'], 'agent')

    def test_list_filters_by_channel_and_state(self):
        a = self.t.create_task('C1', 'A'); b = self.t.create_task('C1', 'B')
        self.t.create_task('C2', 'C')
        self.t.set_state(b, '완료')
        open_c1 = self.t.list_tasks('C1', states=('대기', '진행', '막힘'))
        self.assertEqual({r['id'] for r in open_c1}, {a})

    def test_valid_transition(self):
        tid = self.t.create_task('C1', 'X')
        self.assertTrue(self.t.set_state(tid, '진행'))
        self.assertTrue(self.t.set_state(tid, '완료'))
        self.assertEqual(self.t.get_task(tid)['state'], '완료')

    def test_invalid_transition_rejected(self):
        tid = self.t.create_task('C1', 'X')          # 대기
        self.assertFalse(self.t.set_state(tid, '완료'))  # 대기→완료 금지
        self.assertEqual(self.t.get_task(tid)['state'], '대기')

    def test_terminal_state_is_frozen(self):
        tid = self.t.create_task('C1', 'X')
        self.t.set_state(tid, '취소')
        self.assertFalse(self.t.set_state(tid, '진행'))

    def test_blocked_roundtrip(self):
        tid = self.t.create_task('C1', 'X')
        self.t.set_state(tid, '진행'); self.t.set_state(tid, '막힘')
        self.assertTrue(self.t.set_state(tid, '진행'))   # 해소

if __name__ == '__main__':
    unittest.main()
```

- [ ] **Step 2: 실패 확인**

Run: `python -m unittest tests.test_tasks -v`
Expected: FAIL — `FileNotFoundError`/`ModuleNotFoundError` (tasks.py 없음)

- [ ] **Step 3: 최소 구현** — `templates/scripts/slack-jipsa/tasks.py`

```python
"""jipsa 2.0 작업 객체(Task Layer) — stdlib sqlite3 단일 파일 store.

- 휘발성 슬랙 대화를 상태 가진 task로 승격(대기/진행/막힘/완료/취소).
- 승인 게이트(approval.py)의 랑데부 지점이라 원자성 필수 → SQLite(WAL).
- daemon.py 의 형제 모듈. `import tasks` 로 로드(reminders.py 와 동일 패턴).
"""
from __future__ import annotations

import json
import time
import uuid
import sqlite3
from pathlib import Path

BASE = Path.home() / '.claude/scripts/slack-jipsa'
DB_PATH = BASE / 'jipsa.db'          # 테스트는 이 전역을 임시경로로 교체

STATES = ('대기', '진행', '막힘', '완료', '취소')
DIRECTIONS = ('h2a', 'a2h')          # 사람→에이전트 / 에이전트→사람

# 상태 기계 (설계 A-3). 종결 상태(완료·취소)는 빈 집합 → 동결.
_TRANSITIONS = {
    '대기': {'진행', '막힘', '취소'},
    '진행': {'완료', '막힘', '취소'},
    '막힘': {'진행', '취소'},
    '완료': set(),
    '취소': set(),
}


def _conn() -> sqlite3.Connection:
    DB_PATH.parent.mkdir(parents=True, exist_ok=True)
    conn = sqlite3.connect(str(DB_PATH), timeout=15)
    conn.row_factory = sqlite3.Row
    conn.execute('PRAGMA journal_mode=WAL')     # 데몬+훅+sweeper 동시쓰기 안전
    conn.execute('PRAGMA busy_timeout=15000')
    return conn


def init_db() -> None:
    with _conn() as c:
        c.execute('''CREATE TABLE IF NOT EXISTS tasks (
            id TEXT PRIMARY KEY, channel_id TEXT, title TEXT, body TEXT,
            state TEXT, direction TEXT, assignee TEXT, thread_ts TEXT,
            created_at INTEGER, updated_at INTEGER, meta TEXT)''')
        c.execute('''CREATE TABLE IF NOT EXISTS approvals (
            token TEXT PRIMARY KEY, task_id TEXT, channel_id TEXT,
            action_desc TEXT, status TEXT, approver_id TEXT,
            approvers TEXT, requested_at INTEGER, decided_at INTEGER,
            expires_at INTEGER, thread_ts TEXT)''')
        c.execute('CREATE INDEX IF NOT EXISTS idx_tasks_ch ON tasks(channel_id, state)')


def create_task(channel_id: str, title: str, body: str = '', direction: str = 'h2a',
                assignee: str = 'agent', thread_ts: str = '', meta: dict | None = None) -> str:
    tid = str(uuid.uuid4())
    now = int(time.time())
    with _conn() as c:
        c.execute('INSERT INTO tasks VALUES (?,?,?,?,?,?,?,?,?,?,?)',
                  (tid, channel_id, title, body, '대기', direction, assignee,
                   thread_ts, now, now, json.dumps(meta or {}, ensure_ascii=False)))
    return tid


def get_task(task_id: str) -> dict | None:
    with _conn() as c:
        r = c.execute('SELECT * FROM tasks WHERE id=?', (task_id,)).fetchone()
    return dict(r) if r else None


def list_tasks(channel_id: str, states: tuple[str, ...] | None = None) -> list[dict]:
    q = 'SELECT * FROM tasks WHERE channel_id=?'
    args: list = [channel_id]
    if states:
        q += ' AND state IN (%s)' % ','.join('?' * len(states))
        args += list(states)
    q += ' ORDER BY created_at DESC'
    with _conn() as c:
        return [dict(r) for r in c.execute(q, args).fetchall()]


def set_state(task_id: str, state: str) -> bool:
    """상태 전이. 허용된 전이만 수행하고 성공 여부 반환."""
    if state not in STATES:
        return False
    cur = get_task(task_id)
    if not cur:
        return False
    if state not in _TRANSITIONS.get(cur['state'], set()):
        return False
    with _conn() as c:
        c.execute('UPDATE tasks SET state=?, updated_at=? WHERE id=?',
                  (state, int(time.time()), task_id))
    return True


def update_task(task_id: str, **fields) -> None:
    if not fields:
        return
    cols = ', '.join(f'{k}=?' for k in fields)
    with _conn() as c:
        c.execute(f'UPDATE tasks SET {cols}, updated_at=? WHERE id=?',
                  (*fields.values(), int(time.time()), task_id))
```

- [ ] **Step 4: 통과 확인**

Run: `python -m unittest tests.test_tasks -v`
Expected: PASS (6 tests)

- [ ] **Step 5: 커밋**

```bash
git add templates/scripts/slack-jipsa/tasks.py tests/test_tasks.py
git commit -m "feat(jipsa2): SQLite 작업 객체 store(tasks.py) + 상태기계 + 테스트"
```

---

## Task A2: `daemon.load_channels` — 2.0 필드 파싱 (하위호환)

**Files:**
- Modify: `templates/scripts/slack-jipsa/daemon.py:190-214` (`load_channels`)

- [ ] **Step 1: 구현** — `load_channels` 내 `out[ch] = {...}` 딕셔너리에 신규 필드 3개를 추가한다. 기존 필드는 그대로 두고 아래 3줄만 추가:

```python
                    'require_mention': bool(cfg.get('require_mention', False)),
                    'mission': cfg.get('mission', ''),                     # ← 신규
                    'tasks_enabled': bool(cfg.get('tasks_enabled', False)), # ← 신규
                    'gate': cfg.get('gate') or {},                         # ← 신규(dict; 비면 게이트 off)
```

legacy fallback 반환 딕셔너리(`return {CHANNEL: {...}}`)에도 동일 키를 안전 기본값으로 추가: `'mission': '', 'tasks_enabled': False, 'gate': {}`.

- [ ] **Step 2: 스모크 검증** — 임시 channels.json로 파싱 확인 (실제 데몬 구동 없이):

```bash
python -c "import json,os; from pathlib import Path; \
print('수동 검증: load_channels 가 mission/tasks_enabled/gate 키를 항상 포함하는지 daemon.py 확인')"
```

검증 포인트(코드 리뷰): gate/tasks 필드가 **없는** 기존 channels.json도 KeyError 없이 로드되고 `gate={}`, `tasks_enabled=False`로 떨어지는가 → 1.x 동작 보존.

- [ ] **Step 3: 커밋**

```bash
git add templates/scripts/slack-jipsa/daemon.py
git commit -m "feat(jipsa2): channels.json mission/tasks_enabled/gate 필드 파싱(하위호환)"
```

---

## Task A3: 작업 슬랙 명령 — `작업목록` / `작업 <id> <상태>`

**Files:**
- Modify: `templates/scripts/slack-jipsa/daemon.py` — 상단 import에 `import tasks`(방어적), `handle_message` 내 명령 분기 추가

- [ ] **Step 1: 모듈 로드(방어적)** — `import reminders as rmd` 블록(daemon.py:45-49) 바로 아래에 추가:

```python
# 작업 객체(Task Layer). 로드 실패해도 채팅은 동작하도록 방어.
try:
    import tasks as tsk
    tsk.init_db()
except Exception:
    tsk = None
```

- [ ] **Step 2: 명령 핸들러 함수 추가** — daemon.py의 `handle_help` 근처에 추가:

```python
import re as _re_tasks  # 모듈 상단에 이미 import re 있으면 생략

def _tasks_enabled(channel: str) -> bool:
    return bool(tsk and CHANNELS.get(channel, {}).get('tasks_enabled'))

def handle_task_command(channel: str, text: str) -> bool:
    """작업 관련 슬랙 명령 처리. 처리했으면 True(=이후 claude 호출 skip)."""
    if not _tasks_enabled(channel):
        return False
    t = text.strip()
    if t in ('작업목록', '작업 목록'):
        rows = tsk.list_tasks(channel, states=('대기', '진행', '막힘'))
        if not rows:
            web.chat_postMessage(channel=channel, text='열린 작업이 없어요.')
            return True
        lines = ['*📋 열린 작업*']
        icon = {'대기': '🕒', '진행': '🔧', '막힘': '⛔'}
        for r in rows:
            lines.append(f"{icon.get(r['state'],'•')} `{r['id'][:8]}` {r['title']}  _({r['state']})_")
        web.chat_postMessage(channel=channel, text='\n'.join(lines), mrkdwn=True)
        return True
    m = re.match(r'^작업\s+([0-9a-f]{6,})\s+(진행|막힘|완료|취소)$', t)
    if m:
        prefix, state = m.group(1), m.group(2)
        rows = [r for r in tsk.list_tasks(channel) if r['id'].startswith(prefix)]
        if not rows:
            web.chat_postMessage(channel=channel, text=f'`{prefix}` 작업을 못 찾았어요.')
            return True
        ok = tsk.set_state(rows[0]['id'], state)
        if ok:
            web.chat_postMessage(channel=channel,
                text=f"`{rows[0]['id'][:8]}` → *{state}*", mrkdwn=True)
        else:
            web.chat_postMessage(channel=channel,
                text=f"`{rows[0]['id'][:8]}` 는 현재 *{rows[0]['state']}* 라 *{state}* 로 못 바꿔요.", mrkdwn=True)
        return True
    return False
```

- [ ] **Step 3: 디스패치 연결** — `handle_message` 안에서 도움말 분기(`if HELP_TRIGGER...`) **직전**에 추가:

```python
    # 작업 객체 명령 (tasks_enabled 채널만). 처리되면 claude 호출 skip.
    try:
        if handle_task_command(channel, text):
            return
    except Exception as e:
        log(f'  task cmd err: {e}')
```

- [ ] **Step 4: 스모크 검증** — `tasks_enabled:false`(또는 미설정) 채널에서 `작업목록`을 보내도 `handle_task_command`가 `False`를 반환해 기존 claude 경로로 빠지는지 코드 리뷰로 확인. `tasks_enabled:true`면 DB에 직접 행 INSERT 후 `작업목록` 출력 형식 육안 확인:

```bash
python -c "import sys; sys.path.insert(0,'templates/scripts/slack-jipsa'); \
import tasks; tasks.DB_PATH=__import__('pathlib').Path('/tmp/smoke.db'); tasks.init_db(); \
tid=tasks.create_task('C1','테스트 작업'); print('created', tid[:8]); \
print(tasks.list_tasks('C1', states=('대기','진행','막힘')))"
```
Expected: 생성한 작업 1건 출력.

- [ ] **Step 5: 커밋**

```bash
git add templates/scripts/slack-jipsa/daemon.py
git commit -m "feat(jipsa2): 슬랙 작업 명령(작업목록/작업 <id> <상태>) — tasks_enabled 채널 한정"
```

---

## Task A4: 도움말에 작업 명령 노출 (tasks_enabled 채널만)

**Files:**
- Modify: `templates/scripts/slack-jipsa/daemon.py` `handle_help` (daemon.py:433-450)

- [ ] **Step 1: 구현** — `handle_help` 의 `text = (...)` 마지막 줄 뒤에 조건부 섹션 추가. 함수 끝의 `web.chat_postMessage(...)` 직전에:

```python
    if _tasks_enabled(channel):
        text += (
            "\n\n*📋 작업*\n"
            f"• `{p}작업목록`  _(열린 작업 보기)_\n"
            f"• `{p}작업 ab12cd34 진행|막힘|완료|취소`  _(상태 변경)_"
        )
```

- [ ] **Step 2: 스모크 검증** — `tasks_enabled` 채널에서만 작업 섹션이 붙는지 코드 리뷰.

- [ ] **Step 3: 커밋**

```bash
git add templates/scripts/slack-jipsa/daemon.py
git commit -m "docs(jipsa2): 도움말에 작업 명령 노출(tasks_enabled 채널만)"
```

**▶ Phase A 체크포인트:** 여기서 한 번 끊어 실배포 가능. `tasks_enabled:false`면 1.x와 동일, `true`면 작업 객체 기능만 추가됨.

---

# PHASE B — 개인 채널 승인 게이트 (블라스트 반경 최소)

## Task B1: `approval.py` — 토큰·검증·verdict·Block Kit

**Files:**
- Create: `templates/scripts/slack-jipsa/approval.py`
- Test: `tests/test_approval.py`

- [ ] **Step 1: 실패하는 테스트 작성** — `tests/test_approval.py`

```python
import os, time, tempfile, unittest, importlib.util
from pathlib import Path

ROOT = Path(__file__).resolve().parents[1] / 'templates/scripts/slack-jipsa'

def _load(name, db_path):
    spec = importlib.util.spec_from_file_location(name + '_uut', ROOT / f'{name}.py')
    mod = importlib.util.module_from_spec(spec)
    spec.loader.exec_module(mod)
    mod.DB_PATH = Path(db_path)
    return mod

class ApprovalTest(unittest.TestCase):
    def setUp(self):
        self.tmp = tempfile.mkdtemp()
        db = os.path.join(self.tmp, 'jipsa.db')
        self.t = _load('tasks', db); self.t.init_db()
        self.a = _load('approval', db)   # 같은 DB 공유

    def test_request_creates_pending(self):
        tok = self.a.request_approval('T1', 'C1', 'rm -rf ./build 실행',
                                      approvers=['U_OWNER'], timeout_min=15)
        row = self.a.get_approval(tok)
        self.assertEqual(row['status'], '대기')
        self.assertEqual(row['channel_id'], 'C1')
        self.assertGreater(row['expires_at'], int(time.time()))

    def test_approve_by_authorized(self):
        tok = self.a.request_approval('T1', 'C1', 'x', approvers=['U_OWNER'], timeout_min=15)
        self.assertEqual(self.a.decide(tok, 'U_OWNER'), '승인')
        self.assertEqual(self.a.get_approval(tok)['status'], '승인')
        self.assertEqual(self.a.get_approval(tok)['approver_id'], 'U_OWNER')

    def test_reject_by_unauthorized(self):
        tok = self.a.request_approval('T1', 'C1', 'x', approvers=['U_OWNER'], timeout_min=15)
        self.assertEqual(self.a.decide(tok, 'U_STRANGER'), '권한없음')
        self.assertEqual(self.a.get_approval(tok)['status'], '대기')  # 변동 없음

    def test_single_use_no_double_decide(self):
        tok = self.a.request_approval('T1', 'C1', 'x', approvers=['U_A', 'U_B'], timeout_min=15)
        self.assertEqual(self.a.decide(tok, 'U_A'), '승인')
        self.assertEqual(self.a.decide(tok, 'U_B'), '이미처리')  # 두 번째 클릭 무효

    def test_explicit_reject(self):
        tok = self.a.request_approval('T1', 'C1', 'x', approvers=['U_A'], timeout_min=15)
        self.assertEqual(self.a.decide(tok, 'U_A', approve=False), '거부')
        self.assertEqual(self.a.get_approval(tok)['status'], '거부')

    def test_expiry_sweep(self):
        tok = self.a.request_approval('T1', 'C1', 'x', approvers=['U_A'], timeout_min=0)  # 즉시만료
        time.sleep(0.01)
        self.a.expire_stale()
        self.assertEqual(self.a.get_approval(tok)['status'], '만료')
        self.assertEqual(self.a.decide(tok, 'U_A'), '만료')  # 만료 후 클릭 무효

    def test_build_card_has_token_buttons(self):
        tok = self.a.request_approval('T1', 'C1', 'rm -rf', approvers=['U_A'], timeout_min=15)
        blocks = self.a.build_card(tok, 'rm -rf')
        dumped = str(blocks)
        self.assertIn(tok, dumped)
        self.assertIn('승인', dumped)
        self.assertIn('거부', dumped)

if __name__ == '__main__':
    unittest.main()
```

- [ ] **Step 2: 실패 확인**

Run: `python -m unittest tests.test_approval -v`
Expected: FAIL — approval.py 없음

- [ ] **Step 3: 구현** — `templates/scripts/slack-jipsa/approval.py`

```python
"""jipsa 2.0 승인 게이트 — 토큰 발급·검증, Block Kit 카드, verdict CAS 적용.

검증된 패턴(사직서/수습평가 봇의 버튼→UUID토큰→승인자검증→상태전진)을
데몬 안에서 Python으로 재구현. tasks.py 와 같은 jipsa.db 를 공유한다.
"""
from __future__ import annotations

import json
import time
import uuid
import sqlite3
from pathlib import Path

BASE = Path.home() / '.claude/scripts/slack-jipsa'
DB_PATH = BASE / 'jipsa.db'          # 테스트가 교체하는 전역


def _conn() -> sqlite3.Connection:
    DB_PATH.parent.mkdir(parents=True, exist_ok=True)
    conn = sqlite3.connect(str(DB_PATH), timeout=15)
    conn.row_factory = sqlite3.Row
    conn.execute('PRAGMA busy_timeout=15000')
    return conn


def request_approval(task_id: str, channel_id: str, action_desc: str,
                     approvers: list[str], timeout_min: int, thread_ts: str = '') -> str:
    """approvals 행을 status=대기 로 INSERT 하고 단일사용 토큰 반환."""
    token = str(uuid.uuid4())
    now = int(time.time())
    with _conn() as c:
        c.execute('INSERT INTO approvals (token,task_id,channel_id,action_desc,status,'
                  'approver_id,approvers,requested_at,decided_at,expires_at,thread_ts) '
                  'VALUES (?,?,?,?,?,?,?,?,?,?,?)',
                  (token, task_id, channel_id, action_desc, '대기', '',
                   json.dumps(approvers, ensure_ascii=False), now, 0,
                   now + max(0, int(timeout_min)) * 60, thread_ts))
    return token


def get_approval(token: str) -> dict | None:
    with _conn() as c:
        r = c.execute('SELECT * FROM approvals WHERE token=?', (token,)).fetchone()
    return dict(r) if r else None


def decide(token: str, approver_id: str, approve: bool = True) -> str:
    """버튼 클릭 verdict 적용(compare-and-set).

    반환: '승인' | '거부' | '권한없음' | '이미처리' | '만료' | '없음'
    """
    now = int(time.time())
    with _conn() as c:
        r = c.execute('SELECT * FROM approvals WHERE token=?', (token,)).fetchone()
        if not r:
            return '없음'
        if r['status'] != '대기':
            return '이미처리' if r['status'] in ('승인', '거부') else r['status']
        if now > r['expires_at']:
            c.execute('UPDATE approvals SET status=? WHERE token=? AND status=?',
                      ('만료', token, '대기'))
            return '만료'
        allowed = set(json.loads(r['approvers'] or '[]'))
        if approver_id not in allowed:
            return '권한없음'
        verdict = '승인' if approve else '거부'
        # CAS: status=대기 일 때만 갱신 → 동시 클릭 시 첫 verdict만 채택
        cur = c.execute('UPDATE approvals SET status=?, approver_id=?, decided_at=? '
                        'WHERE token=? AND status=?',
                        (verdict, approver_id, now, token, '대기'))
        if cur.rowcount == 0:
            return '이미처리'
    return verdict


def expire_stale() -> int:
    """기한 지난 대기 행을 만료 처리. 만료된 건수 반환(요청자 알림용)."""
    now = int(time.time())
    with _conn() as c:
        cur = c.execute('UPDATE approvals SET status=? WHERE status=? AND expires_at < ?',
                        ('만료', '대기', now))
        return cur.rowcount


def list_expired_since(since_ts: int) -> list[dict]:
    """sweeper가 알림 보낼 대상: 최근 만료된 행."""
    with _conn() as c:
        rows = c.execute('SELECT * FROM approvals WHERE status=? AND expires_at >= ?',
                         ('만료', since_ts)).fetchall()
    return [dict(r) for r in rows]


def build_card(token: str, action_desc: str) -> list:
    """슬랙 Block Kit 승인 카드. 버튼 value 에 token 바인딩."""
    return [
        {'type': 'section', 'text': {'type': 'mrkdwn',
            'text': f'🔐 *승인 요청*\n```{action_desc[:500]}```\n_승인하면 실행, 거부하면 차단합니다._'}},
        {'type': 'actions', 'block_id': f'gate_{token}', 'elements': [
            {'type': 'button', 'style': 'primary', 'text': {'type': 'plain_text', 'text': '✅ 승인'},
             'action_id': 'gate_approve', 'value': token},
            {'type': 'button', 'style': 'danger', 'text': {'type': 'plain_text', 'text': '⛔ 거부'},
             'action_id': 'gate_reject', 'value': token},
        ]},
    ]
```

- [ ] **Step 4: 통과 확인**

Run: `python -m unittest tests.test_approval -v`
Expected: PASS (7 tests)

- [ ] **Step 5: 커밋**

```bash
git add templates/scripts/slack-jipsa/approval.py tests/test_approval.py
git commit -m "feat(jipsa2): 승인 게이트 코어(approval.py) — 토큰/CAS verdict/만료/카드 + 테스트"
```

---

## Task B2: `pretooluse_gate.py` — PreToolUse 훅(별도 프로세스)

**Files:**
- Create: `templates/scripts/slack-jipsa/pretooluse_gate.py`

**동작:** Claude가 민감 도구 호출 직전 이 훅을 별도 프로세스로 띄운다. 훅은 ① `approvals` INSERT + 슬랙 카드 게시, ② status 바뀔 때까지 DB 폴링, ③ 승인이면 allow JSON, 거부/만료면 deny JSON 출력. 데몬과 메모리 결합 없음.

- [ ] **Step 1: 구현** — `templates/scripts/slack-jipsa/pretooluse_gate.py`

```python
#!/usr/bin/env python3
"""PreToolUse 게이트 훅 — 민감 도구 호출 직전 슬랙 승인 대기.

Claude Code 가 별도 프로세스로 실행. stdin=PreToolUse payload(JSON).
출력: {"hookSpecificOutput":{"hookEventName":"PreToolUse",
       "permissionDecision":"allow"|"deny","permissionDecisionReason":...}} (exit 0)

게이트 컨텍스트(채널/승인자/타임아웃)는 데몬이 env 로 주입:
  JIPSA_GATE_CHANNEL, JIPSA_GATE_THREAD, JIPSA_GATE_APPROVERS(쉼표구분),
  JIPSA_GATE_TIMEOUT_MIN
env 가 없으면(=비게이트 컨텍스트) 즉시 allow 로 통과(안전한 무동작).
"""
from __future__ import annotations

import os
import sys
import json
import time
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent))
import approval  # 같은 디렉토리 형제 모듈
import tasks


def _allow(reason: str = '') -> None:
    out = {'hookSpecificOutput': {'hookEventName': 'PreToolUse',
           'permissionDecision': 'allow'}}
    if reason:
        out['hookSpecificOutput']['permissionDecisionReason'] = reason
    print(json.dumps(out))
    sys.exit(0)


def _deny(reason: str) -> None:
    print(json.dumps({'hookSpecificOutput': {'hookEventName': 'PreToolUse',
          'permissionDecision': 'deny', 'permissionDecisionReason': reason}}))
    sys.exit(0)


def _describe(tool_name: str, tool_input: dict) -> str:
    if tool_name == 'Bash':
        return f"Bash: {tool_input.get('command', '')[:400]}"
    if tool_name in ('Write', 'Edit', 'MultiEdit', 'NotebookEdit'):
        return f"{tool_name}: {tool_input.get('file_path', '')}"
    return f"{tool_name}: {json.dumps(tool_input, ensure_ascii=False)[:300]}"


def _post_card(channel: str, thread_ts: str, token: str, desc: str) -> None:
    """데몬과 독립적으로 슬랙에 카드 게시(봇 토큰 직접 사용)."""
    from slack_sdk import WebClient
    secrets = Path.home() / '.claude/secrets/slack-jipsa.env'
    env = {}
    for line in secrets.read_text(encoding='utf-8').splitlines():
        if '=' in line and not line.startswith('#'):
            k, v = line.split('=', 1)
            env[k.strip()] = v.strip()
    web = WebClient(token=env['SLACK_BOT_TOKEN'])
    web.chat_postMessage(channel=channel, blocks=approval.build_card(token, desc),
                         text='🔐 승인 요청', thread_ts=thread_ts or None)


def main() -> None:
    channel = os.environ.get('JIPSA_GATE_CHANNEL', '')
    if not channel:
        _allow()                       # 게이트 컨텍스트 아님 → 통과
    try:
        payload = json.load(sys.stdin)
    except Exception:
        _allow('payload 파싱 실패 — 안전 통과')
    tool_name = payload.get('tool_name', '')
    tool_input = payload.get('tool_input', {}) or {}
    desc = _describe(tool_name, tool_input)
    approvers = [a for a in os.environ.get('JIPSA_GATE_APPROVERS', '').split(',') if a]
    timeout_min = int(os.environ.get('JIPSA_GATE_TIMEOUT_MIN', '15') or '15')
    thread_ts = os.environ.get('JIPSA_GATE_THREAD', '')

    tasks.init_db()
    # 게이트 1건 = task(막힘) + approval(대기)
    tid = tasks.create_task(channel, f'승인대기: {desc[:60]}', body=desc,
                            direction='a2h', thread_ts=thread_ts,
                            meta={'gate': True})
    tasks.set_state(tid, '진행'); tasks.set_state(tid, '막힘')
    token = approval.request_approval(tid, channel, desc, approvers, timeout_min, thread_ts)
    try:
        _post_card(channel, thread_ts, token, desc)
    except Exception as e:
        _allow(f'카드 게시 실패({e}) — 운영중단 방지 위해 통과')  # fail-open: 게이트가 작업을 막지 않게

    # DB 폴링 랑데부 (훅 timeout 은 settings 에서 timeout_min*60+여유 로 설정)
    deadline = time.time() + timeout_min * 60 + 5
    while time.time() < deadline:
        row = approval.get_approval(token)
        st = row['status'] if row else '없음'
        if st == '승인':
            tasks.set_state(tid, '진행')
            _allow('슬랙 승인됨')
        if st in ('거부', '만료'):
            _deny(f'슬랙에서 {st} 처리됨')
        time.sleep(2)
    approval.expire_stale()
    _deny('승인 타임아웃 — 기본 거부')


if __name__ == '__main__':
    main()
```

> **fail-open vs fail-closed 결정:** 카드 게시 실패 시 *통과(allow)* 한다. 게이트 인프라 장애가 개인 비서를 마비시키지 않게 하는 선택. 단, 타임아웃은 설계대로 *거부(deny)* — 사람이 못 본 민감작업은 막는다. 이 비대칭은 의도된 것.

- [ ] **Step 2: 스모크 검증** — env 없이 실행하면 즉시 allow 하는지(비게이트 안전):

```bash
echo '{"tool_name":"Bash","tool_input":{"command":"ls"}}' | python templates/scripts/slack-jipsa/pretooluse_gate.py
```
Expected: `{"hookSpecificOutput": {"hookEventName": "PreToolUse", "permissionDecision": "allow"}}`, exit 0.

- [ ] **Step 3: 커밋**

```bash
git add templates/scripts/slack-jipsa/pretooluse_gate.py
git commit -m "feat(jipsa2): PreToolUse 게이트 훅 — 카드 게시+DB 폴링 랑데부+allow/deny"
```

---

## Task B3: `.claude/settings.json.tmpl` — 훅 등록

**Files:**
- Create: `templates/scripts/slack-jipsa/.claude/settings.json.tmpl`

- [ ] **Step 1: 구현** — 게이트 채널의 cwd(slack-jipsa)에서 claude가 자동 로드. `__PYTHON__`는 설치 시 OS별 치환(Windows=`python`, Unix=`python3`). `timeout`은 개인 채널 15분(=900s) + 여유.

```json
{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash|Write|Edit|MultiEdit|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "__PYTHON__ \"$CLAUDE_PROJECT_DIR/pretooluse_gate.py\"",
            "timeout": 1000
          }
        ]
      }
    ]
  }
}
```

- [ ] **Step 2: 스모크 검증** — JSON 유효성:

```bash
python -c "import json; json.load(open('templates/scripts/slack-jipsa/.claude/settings.json.tmpl'))" && echo OK
```
Expected: `OK` (단, `__PYTHON__`/`$CLAUDE_PROJECT_DIR`는 문자열이라 JSON 파싱엔 영향 없음).

- [ ] **Step 3: 커밋**

```bash
git add templates/scripts/slack-jipsa/.claude/settings.json.tmpl
git commit -m "feat(jipsa2): PreToolUse 게이트 훅 등록 settings 템플릿(timeout 연장)"
```

---

## Task B4: `_run_claude` 게이트 배선 — CLAUDE_SKIP_HOOKS 조건부 + env 주입

**Files:**
- Modify: `templates/scripts/slack-jipsa/daemon.py:252-300` (`_run_claude`, `call_claude`)

- [ ] **Step 1: `_run_claude` 시그니처/본문 변경** — `gate: dict | None = None`, `gate_channel`/`gate_thread` 인자 추가. 본문 상단의 env 설정을 다음으로 교체:

```python
    env = os.environ.copy()
    if not gate:
        env['CLAUDE_SKIP_HOOKS'] = '1'          # 기존 동작: 훅 전부 끔
    else:
        # 게이트 채널: 훅 켜고 게이트 컨텍스트 주입
        env.pop('CLAUDE_SKIP_HOOKS', None)
        env['JIPSA_GATE_CHANNEL'] = gate_channel or ''
        env['JIPSA_GATE_THREAD'] = gate_thread or ''
        env['JIPSA_GATE_APPROVERS'] = ','.join(gate.get('approvers', []))
        env['JIPSA_GATE_TIMEOUT_MIN'] = str(gate.get('timeout_min', 15))
```

함수 인자에 추가: `gate: dict | None = None, gate_channel: str = '', gate_thread: str = ''`.

- [ ] **Step 2: `call_claude` 에서 게이트 판정 후 전달** — `call_claude` 시그니처에 `thread_ts: str = ''` 추가하고, cfg 로드부 아래에 게이트 계산:

```python
    gate_cfg = cfg.get('gate') or {}
    use_gate = bool(gate_cfg.get('enabled')) and gate_cfg.get('mode', 'block') != 'escalate'
    gate_arg = gate_cfg if use_gate else None
```

그리고 두 곳의 `_run_claude(...)` 호출에 `gate=gate_arg, gate_channel=channel, gate_thread=thread_ts` 를 추가한다.

> Phase B는 개인 채널 차단 게이트만(`mode != 'escalate'`). 부서 escalate(`mode:'escalate'`)는 Phase C에서 처리하므로 여기선 게이트를 켜지 않는다(use_gate=False).

- [ ] **Step 3: `handle_message` 의 `call_claude` 호출에 thread_ts 전달** — daemon.py:718 `reply = call_claude(prompt_with_ctx, channel)` 를:

```python
    reply = call_claude(prompt_with_ctx, channel, thread_ts=thread_ts)
```
(thread_ts 는 같은 함수 내 이미 정의됨: daemon.py:693)

- [ ] **Step 4: 스모크 검증(코드 리뷰)** — ① gate 미설정 채널은 `gate_arg=None` → `CLAUDE_SKIP_HOOKS='1'` 설정 → 1.x 동작 동일. ② summarize/pin 의 `_run_claude` 호출은 gate 인자 없음 → 기본 None → 영향 없음. ③ `mode:'escalate'`도 Phase B에선 게이트 off.

- [ ] **Step 5: 커밋**

```bash
git add templates/scripts/slack-jipsa/daemon.py
git commit -m "feat(jipsa2): 게이트 채널만 CLAUDE_SKIP_HOOKS 해제 + 게이트 env 주입"
```

---

## Task B5: 데몬 block_actions(버튼) 핸들러

**Files:**
- Modify: `templates/scripts/slack-jipsa/daemon.py` `on_event` (daemon.py:779-789) + 신규 `handle_block_action`

- [ ] **Step 1: `on_event` 에 interactive 분기 추가** — `if req.type != 'events_api': return` 줄을 다음으로 교체:

```python
    if req.type == 'interactive':
        payload = req.payload or {}
        if payload.get('type') == 'block_actions':
            threading.Thread(target=handle_block_action, args=(payload,), daemon=True).start()
        return
    if req.type != 'events_api':
        return
```

- [ ] **Step 2: `handle_block_action` 추가** — `on_event` 위에 정의:

```python
def handle_block_action(payload: dict) -> None:
    """승인 게이트 버튼 클릭 처리. token=button value, 누른 사람 검증 후 verdict."""
    if tsk is None:
        return
    try:
        import approval as apv
    except Exception as e:
        log(f'  approval import fail: {e}')
        return
    user = (payload.get('user') or {}).get('id', '')
    actions = payload.get('actions') or []
    if not actions:
        return
    act = actions[0]
    action_id = act.get('action_id', '')
    token = act.get('value', '')
    channel = (payload.get('channel') or {}).get('id', '')
    msg_ts = (payload.get('message') or {}).get('ts', '')
    if action_id not in ('gate_approve', 'gate_reject') or not token:
        return
    approve = (action_id == 'gate_approve')
    result = apv.decide(token, user, approve=approve)
    name = _resolve_name(user)
    if result in ('승인', '거부'):
        # 카드 메시지를 결과로 교체(버튼 제거)
        try:
            web.chat_update(channel=channel, ts=msg_ts,
                text=f"{'✅' if result=='승인' else '⛔'} {name}님이 *{result}* 했습니다.",
                blocks=[{'type': 'section', 'text': {'type': 'mrkdwn',
                    'text': f"{'✅' if result=='승인' else '⛔'} {name}님이 *{result}* 했습니다."}}])
        except Exception as e:
            log(f'  card update fail: {e}')
        log(f'gate {result} token={token[:8]} by={name}')
        # 감사: 노션 적재(있으면). Task B7.
        try:
            _audit_verdict(token, result, user, name)
        except Exception:
            pass
    elif result == '권한없음':
        try:
            web.chat_postEphemeral(channel=channel, user=user,
                text='이 승인 요청을 처리할 권한이 없어요.')
        except Exception:
            pass
    else:  # 이미처리 / 만료 / 없음
        try:
            web.chat_postEphemeral(channel=channel, user=user,
                text=f'이미 처리됐거나 만료된 요청이에요. ({result})')
        except Exception:
            pass
```

- [ ] **Step 3: `_audit_verdict` 스텁 추가**(Task B7에서 채움) — 우선 무동작:

```python
def _audit_verdict(token: str, result: str, user: str, name: str) -> None:
    """승인 verdict 감사 로그. Task B7 에서 노션 적재 연결."""
    pass
```

- [ ] **Step 4: 슬랙 앱 스코프 확인 안내(코드 아님, 검증 노트)** — block_actions(interactive) 이벤트를 받으려면 Socket Mode 의 **Interactivity** 가 켜져 있어야 한다. 이 요구사항을 module 09에 명시(Task D2).

- [ ] **Step 5: 커밋**

```bash
git add templates/scripts/slack-jipsa/daemon.py
git commit -m "feat(jipsa2): 데몬 block_actions 버튼 핸들러 — 승인/거부 verdict 적용"
```

---

## Task B6: 만료 sweeper 스레드 + 요청자 알림

**Files:**
- Modify: `templates/scripts/slack-jipsa/daemon.py` `main` (daemon.py:913-927)

- [ ] **Step 1: sweeper 함수 추가** — `main` 위에:

```python
def _gate_sweeper() -> None:
    """주기적으로 만료된 승인 요청을 정리하고 막힌 task 를 알림."""
    if tsk is None:
        return
    try:
        import approval as apv
    except Exception:
        return
    last = int(time.time())
    while True:
        time.sleep(30)
        try:
            n = apv.expire_stale()
            if n:
                for row in apv.list_expired_since(last):
                    ch = row.get('channel_id'); th = row.get('thread_ts') or None
                    try:
                        web.chat_postMessage(channel=ch, thread_ts=th,
                            text=f"⏱️ 승인 요청이 만료돼 *거부* 처리됐어요: {row.get('action_desc','')[:120]}")
                    except Exception:
                        pass
            last = int(time.time())
        except Exception as e:
            log(f'  sweeper err: {e}')
```

- [ ] **Step 2: `main` 에서 스레드 시작** — 알리미 스레드 시작부 근처에 추가:

```python
    threading.Thread(target=_gate_sweeper, daemon=True).start()
```

- [ ] **Step 3: 스모크 검증** — `expire_stale`/`list_expired_since`가 test_approval(Task B1)에서 이미 검증됨. 데몬 통합은 육안.

- [ ] **Step 4: 커밋**

```bash
git add templates/scripts/slack-jipsa/daemon.py
git commit -m "feat(jipsa2): 게이트 만료 sweeper 스레드 + 요청자 알림"
```

---

## Task B7: 감사 로그 — verdict 를 노션에 적재(있으면) + 더블로깅 점검

**Files:**
- Modify: `templates/scripts/slack-jipsa/daemon.py` `_audit_verdict`

- [ ] **Step 1: `_audit_verdict` 구현** — `NOTION_SESSION_DB` 비면 skip(기존 패턴 재사용). 기존 `notion_log_turn` 의 토큰 로딩/HTTP 헬퍼와 동일 방식으로 한 행 적재. 최소 구현:

```python
def _audit_verdict(token: str, result: str, user: str, name: str) -> None:
    if not NOTION_SESSION_DB or tsk is None:
        return
    try:
        import approval as apv
        row = apv.get_approval(token) or {}
        threading.Thread(target=notion_log_turn, args=(
            row.get('channel_id', ''), f'gate:{token}',
            f'승인요청: {row.get("action_desc","")}',
            f'{name}({user}) → {result}',
            f'gate:{token}', 'gate'), daemon=True).start()
    except Exception as e:
        log(f'  audit fail: {e}')
```

- [ ] **Step 2: 더블로깅 스모크 점검** — 게이트 채널에서 1턴 돌린 뒤 노션 세션 DB에 동일 ext_id 중복 행이 생기는지 확인. `notion_log_turn` 은 `upsert_by_external_id(ext_id=jipsa:{channel}:{event_ts})` 라 멱등. 사용자 `~/.claude/settings.json` 에 Stop 훅이 있어 추가 행이 생기면, Task B3 settings.json.tmpl 에 빈 `"Stop": []` 를 추가해 무력화(아래 대안). **검증 결과를 module 09 트러블슈팅에 기록.**

대안(필요 시) settings.json.tmpl 에 추가:
```json
    "Stop": []
```

- [ ] **Step 3: 커밋**

```bash
git add templates/scripts/slack-jipsa/daemon.py
git commit -m "feat(jipsa2): 승인 verdict 감사 로그(노션, 옵션) + 더블로깅 점검"
```

**▶ Phase B 통합 스모크 (실환경, 수동):** 개인 채널 channels.json에 `gate.enabled:true` + `approvers:[내 slack_id]` 설정 → 데몬 재시작 → 개인 채널에서 "test.txt 파일 하나 만들어줘" → 슬랙에 `[✅승인][⛔거부]` 카드 등장 확인 → 승인 → 파일 생성됨 / 거부 → 차단됨 / 무응답 15분 → 만료 거부 + 알림. 데몬 죽였다 살려도 폴링 복구되는지 확인(새벽 3시 원칙).

---

# 문서 (AI 가이드 — 키트의 절반)

## Task D1: `channels.json.example` 2.0 필드 추가

**Files:**
- Modify: `templates/scripts/slack-jipsa/channels.json.example`

- [ ] **Step 1: 구현** — 개인 채널 블록에 `mission`/`tasks_enabled`/`gate` 추가, 부서 채널에 `mission`/`tasks_enabled` 추가(부서 gate는 Phase C이므로 주석으로만 언급):

```jsonc
{
  "C_PERSONAL_CHANNEL_ID": {
    "label": "개인 비서",
    "model": "opus",
    "access": "owner",
    "cwd": "~/.claude/scripts/slack-jipsa",
    "add_dirs": ["~"],
    "mission": "오롯이 내 개인 비서. 빌드/배포 보조.",
    "tasks_enabled": true,
    "gate": {
      "enabled": false,
      "sensitive_tools": ["Bash", "Write", "Edit"],
      "approvers": ["U_OWNER_SLACK_ID"],
      "timeout_min": 15,
      "on_timeout": "deny"
    }
  },
  "C_TEAM_CHANNEL_ID": {
    "label": "부서 공용 비서",
    "model": "sonnet",
    "access": "all",
    "cwd": "~/.claude/scripts/slack-team",
    "add_dirs": [],
    "disallowed_tools": ["Bash", "Write", "Edit", "MultiEdit", "NotebookEdit"],
    "require_mention": true,
    "mission": "부서 공용 집사. 알리미/FAQ/투표/요약/온보딩.",
    "tasks_enabled": true
  }
}
```

> `gate.enabled` 기본 `false` — 켜기 전까지 1.x 동작 그대로. 검증 후 `true`.

- [ ] **Step 2: 검증** — `python -c "import json; json.load(open('templates/scripts/slack-jipsa/channels.json.example'))"` (jsonc 주석 없는 순수 JSON으로 유지). 통과.

- [ ] **Step 3: 커밋**

```bash
git add templates/scripts/slack-jipsa/channels.json.example
git commit -m "docs(jipsa2): channels.json 예시에 mission/tasks_enabled/gate 추가"
```

## Task D2: `modules/08-task-layer.md` + `modules/09-approval-gate.md`

**Files:**
- Create: `modules/08-task-layer.md`, `modules/09-approval-gate.md`

- [ ] **Step 1: 08 작성** — 작업 객체 개념(상태기계), `tasks_enabled` 켜는 법, 슬랙 명령(`작업목록`/`작업 <id> <상태>`), `jipsa.db` 위치/백업, "1.x 기능은 그대로 두고 신규부터 얹는다" 최소 diff 원칙. 기존 모듈(05/06/07)의 문체·구조 따름.

- [ ] **Step 2: 09 작성** — 승인 게이트: ① `gate` 필드 의미, ② **슬랙 앱 Interactivity(Socket Mode) 켜기** + 필요 스코프(`chat:write`는 기존), ③ `.claude/settings.json` 의 `__PYTHON__` 치환(Windows `python`/Unix `python3`), ④ `approvers` 에 본인 slack_id 넣기, ⑤ 단계적 롤아웃(`enabled:false`→테스트→`true`), ⑥ 트러블슈팅(카드 안 뜸=Interactivity 미설정/CLAUDE_SKIP_HOOKS, 더블 노션로그=Stop훅 → `"Stop":[]`), ⑦ 새벽3시 원칙(DB 복구) 설명.

- [ ] **Step 3: 검증** — 마크다운 육안 + 기존 모듈과 톤 일치.

- [ ] **Step 4: 커밋**

```bash
git add modules/08-task-layer.md modules/09-approval-gate.md
git commit -m "docs(jipsa2): 모듈 08(작업 객체)·09(승인 게이트) 셋업 가이드"
```

## Task D3: `SKILL.md` + `README.md` 반영

**Files:**
- Modify: `SKILL.md`, `README.md`

- [ ] **Step 1: SKILL.md** — 모듈 선택 메뉴에 08/09 추가, 셋업 흐름에 게이트 옵션 단계 삽입(개인 채널 셋업 후 "승인 게이트 켤까요?" 분기). 설치 시 `.claude/settings.json.tmpl` 의 `__PYTHON__` 치환 단계 명시.

- [ ] **Step 2: README.md** — "무엇을 깔아주나요" 표에 작업 객체·승인 게이트 행 추가, 폴더 구조에 신규 파일(tasks.py/approval.py/pretooluse_gate.py/.claude/settings.json) 반영, 디자인 원칙에 "휘발성 대화 → 추적 가능 작업 + 양방향" 한 줄 추가.

- [ ] **Step 3: 검증** — 링크/표 육안. README 클론 URL(lenisark) 유지.

- [ ] **Step 4: 커밋**

```bash
git add SKILL.md README.md
git commit -m "docs(jipsa2): SKILL/README 에 작업 객체·승인 게이트 반영"
```

---

## Self-Review (작성자 체크리스트)

**1. 설계(JIPSA-2.0-DESIGN.md) 커버리지:**
- A-1 SQLite 단일파일 → Task A1 ✅ (경로만 co-locate로 조정, 결정#2)
- A-2 스키마(tasks/approvals) → Task A1 ✅ (approvals 에 approvers·thread_ts 추가 — 토큰 바인딩/스레드 앵커용)
- A-3 상태기계 → Task A1 `_TRANSITIONS` ✅
- A-4 인터페이스(작업목록/상태변경, a2h 스레드) → Task A3 ✅ (a2h 자동 스레드는 게이트 task가 thread_ts 사용 — Task B2)
- B-1 개인 차단 게이트 → Task B2~B6 ✅ / 부서 escalate → Phase C(범위 외, 명시)
- B-2 버튼→토큰→검증→전진 → approval.decide + handle_block_action ✅
- B-3 DB 랑데부 → pretooluse_gate 폴링 ✅
- B-4 보안(단일사용·승인자검증·타임아웃·감사) → decide/expire_stale/_audit_verdict ✅
- B-5 엣지케이스(동시클릭 CAS·재시작복구·만료후클릭·리플레이·중첩호출) → decide CAS + WAL + 만료 + matcher 전역 ✅
- C-1 channels.json 2.0 → Task A2 + D1 ✅
- C-2 신규파일 → 전부 매핑 ✅ (위치만 형제파일로 조정, 결정#1)

**2. 플레이스홀더 스캔:** 없음. `_audit_verdict`는 B5에서 스텁→B7에서 구현(의도된 점진, 코드 포함). `__PYTHON__`은 설치 치환 토큰(문서화됨).

**3. 타입 일관성:** `set_state`/`create_task`/`get_task`/`list_tasks`(tasks.py) 시그니처가 A3·B2에서 동일하게 사용됨. `request_approval`/`get_approval`/`decide`/`expire_stale`/`list_expired_since`/`build_card`(approval.py)가 B2·B5·B6에서 동일 시그니처로 호출됨. `decide`의 `approve` 인자, 반환 문자열('승인'/'거부'/'권한없음'/'이미처리'/'만료'/'없음')이 테스트·핸들러에서 일치.

**범위 외(명시적 비목표):** 웹 대시보드/MCP재설계/SaaS.

---

# PHASE C — 부서 escalate 게이트 (구현 완료)

기존 PreToolUse 게이트를 재사용. 차이는 셋뿐:
- `call_claude`: `use_gate`를 escalate에도 적용. escalate면 `gate.sensitive_tools`를 `disallowed_tools`에서 제거(하드차단 해제) → 그 도구는 게이트를 거쳐야만 실행. 나머지(예: Bash)는 계속 차단.
- `_run_claude`: `JIPSA_GATE_MODE` env 추가. pretooluse_gate가 escalate면 `build_card(..., mentions=approvers)`로 승인자 `<@UID>` 호출.
- `settings.json.tmpl`: 명령 경로를 `$CLAUDE_PROJECT_DIR` → 절대경로 `__GATE_DIR__`로 변경 → 부서 채널 cwd(slack-team)에서도 동일 훅 파일(slack-jipsa의 pretooluse_gate.py) 실행. slack-team에는 같은 settings.json만 배치(훅 파일 복사 불필요).
- `channels.json.example`: 부서 항목에 `gate.mode:escalate` 추가.
- 테스트: `test_build_card_mentions_approvers` 추가(총 approval 8건).

# PHASE D — 알리미 2.0 능동 실행 (구현 완료)

`reminders.py`에 "실행+보고"를 얹음(스케줄/공휴일/캐치업 로직 재사용):
- 능동 작업 감지(결정적): 스케줄 표현 + (`자동` 마커 OR 작업동사`요약|정리|...` + 보고동사`올려|게시|...`). 단순 핑은 1.x 동작 유지.
- `parse_intent`: 능동 작업이면 `action`(지시문) 추출(`_extract_action` — 스케줄/시간만 제거, 작업지시 보존). `add_reminder`가 영속화.
- `set_executor(fn)`: 데몬이 `run_scheduled_action(channel, prompt)` 주입. `_post_fire`는 action 있으면 **lock 밖 비동기 스레드**로 실행 후 결과 게시(본 알림만, 사전알림 제외).
- 데몬 `run_scheduled_action`: 새 세션·게이트 없음(등록 시 사전승인)·채널 disallowed_tools 유지(부서=읽기전용, 개인=풀권한)·mrkdwn 정리 후 반환.
- `modules/10-active-scheduler.md` 신규. 테스트 `tests/test_reminders.py`(5건) — 액션 감지/추출/오인식방지.

**위생 개선:** `tasks.py`·`approval.py`의 모든 `_conn()`을 `contextlib.closing`으로 감싸 커넥션 누수 제거(장기 실행 데몬). `python -W error::ResourceWarning` 로 검증.

**전체 검증:** unittest 19건(tasks 6 + approval 8 + reminders 5) 통과 + 게이트 랑데부/Phase D 발사 통합 스모크 통과. 라이브(실제 슬랙) 검증은 별도.
